NODE_ENV=production
PORT=3001
WEB_ORIGIN=https://vivorax.online,https://www.vivorax.online,https://egyhost1.com,http://localhost:8080,http://localhost:5173
JWT_SECRET=replace-with-a-long-random-secret-at-least-64-characters
OPENROUTER_API_KEY=your-openrouter-api-key
# Keep these only in server/.env — never use VITE_ variables for Modal tokens.
MODAL_TOKEN_ID=ak-your-modal-token-id
MODAL_TOKEN_SECRET=as-your-modal-token-secret
ADMIN_EMAIL=owner@egyhost1.com
# Keep this folder outside public_html on cPanel when possible.
DATA_DIR=./data
