/**
 * The legacy player persisted its playlist through an external service. It remains hidden
 * until playlist storage is implemented through Webo's local server API.
 */
export const FloatingMusicPlayer = () => null;
