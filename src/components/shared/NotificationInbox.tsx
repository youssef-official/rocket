/**
 * Inbox delivery is pending migration to Vivora X's local API. Rendering nothing is
 * intentional: it avoids invoking removed external notification tables.
 */
export const NotificationInbox = () => null;
